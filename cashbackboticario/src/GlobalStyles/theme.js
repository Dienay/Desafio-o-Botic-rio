import { createMuiTheme } from '@material-ui/core/styles';

export const BaseTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#484CAD",
    },
    secondary: {
      main: "#fff",
    },
  },
});